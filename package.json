{
  "devDependencies": {
    "concurrently": "^5.0.0",
    "typescript": "^3.7.2"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  },
  "dependencies": {
    "axios": "^0.30.0",
    "json-server": "^0.15.1"
  }
}
